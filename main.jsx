// main.jsx - placeholderimport React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './css/styles.css';
const root = ReactDOM.createRoot(document.getElementById('root'));

// Make sure that `BrowserRouter` is only in **App.jsx** and not here.
root.render(
  <App />
);
